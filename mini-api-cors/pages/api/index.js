export default function handler(req, res) {
    res.status(200).json({
        platform: 'Back End',
        name: 'Mario',
        message: 'DESCONFIGURA O COTONETE EM CIMA DO BOTÃO DE LIKE'
    })
}